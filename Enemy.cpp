#include "Enemy.h"

Enemy::Enemy() {
    // Initialise enemy properties
}

void Enemy::update(float deltaTime) {
    // Update enemy position and state
}

void Enemy::render() {
    // Renders the enemy
}
